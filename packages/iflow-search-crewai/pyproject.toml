[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "iflow-search-crewai"
version = "0.1.0"
description = "CrewAI tools for iFlow Search (心流搜索) — web search, image search, and web-page fetching."
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.10"
authors = [{ name = "iFlow Search SDK contributors" }]
keywords = ["iflow", "search", "crewai", "ai", "llm", "tools", "agents"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Internet :: WWW/HTTP",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Typing :: Typed",
]
dependencies = [
    "iflow-search>=0.1.0,<0.2",
    "crewai>=0.80.0,<2.0",
    "pydantic>=2.7,<3.0",
]

[project.urls]
Homepage = "https://platform.iflow.cn/"
Documentation = "https://platform.iflow.cn/docs/"
Repository = "https://github.com/zhengyanglsun/iflow-search-py"
Issues = "https://github.com/zhengyanglsun/iflow-search-py/issues"

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "httpx>=0.27,<1.0",
    "ruff>=0.4",
    "mypy>=1.10",
    "tomli>=2.0; python_version<'3.11'",
]

[tool.hatch.build.targets.wheel]
packages = ["src/iflow_search_crewai"]

[tool.hatch.build.targets.sdist]
include = [
    "/src",
    "/README.md",
    "/LICENSE",
    "/pyproject.toml",
]
exclude = [
    "/tests",
    "/scripts",
    "/.pytest_cache",
    "/__pycache__",
]

[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
addopts = "-ra --strict-markers"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.10"
strict = true
warn_unused_ignores = true
files = ["src/iflow_search_crewai"]
